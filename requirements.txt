# バージョンを完全固定している理由:
#   このファイルは GitHub Actions 上で `pip install` され、その実行環境には
#   将来アフィリエイトIDなどを Secret として渡す可能性がある。範囲指定(>=)の
#   ままだと上流パッケージが改ざんされた新バージョンをCIが自動で取り込み、
#   Secret に到達され得る。
#
# 更新するときは .github/dependabot.yml が出すPRを取り込むこと。
# 手で上げる場合は既知の脆弱性を踏まないよう最新版を確認してから固定する。
#
# HTML のパースに BeautifulSoup / lxml を使っていないのは意図的。
# 標準ライブラリの re と html で足りる範囲に収めて、CI に持ち込む
# サードパーティ製コードを減らしている。

# --- 直接依存 ---
requests==2.34.2
Jinja2==3.1.6
PyYAML==6.0.3

# --- 推移的依存(これらも固定しないと範囲指定で入ってくる) ---
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.19
MarkupSafe==3.0.3
urllib3==2.7.0
